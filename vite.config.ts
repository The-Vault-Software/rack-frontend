import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    css: true,
    coverage: {
      provider: 'v8',
      reportsDirectory: './coverage',
      include: ['src/**/*.{ts,tsx}'],
      // src/client is generated by openapi-ts and is never hand-written.
      exclude: ['src/client/**', 'src/test/**', 'src/**/*.d.ts', 'src/main.tsx'],
    },
  },
})
