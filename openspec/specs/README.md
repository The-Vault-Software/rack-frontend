# Specs

Source-of-truth specifications for `rack-frontend`. Each domain lives in its own
subdirectory (e.g., `auth/spec.md`, `inventory/spec.md`).

Specs here are the cumulative result of archived SDD changes. Active changes keep
their delta specs under `openspec/changes/{change-name}/specs/` until archived.